import type { Draw, PredictionResult } from "@/types";

/**
 * Placeholder probability engine. Will produce a ranked PredictionResult[].
 */
export function computeProbabilityRanking(_draws: Draw[]): PredictionResult[] {
  return [];
}
